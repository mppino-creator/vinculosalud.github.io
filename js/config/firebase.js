// js/config/firebase.js
// Firebase ya está inicializado en el HTML con las etiquetas script compat.
// Solo exportamos la referencia a la base de datos.

const db = firebase.database();
export { db };